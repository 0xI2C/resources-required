[Unit]
Description=Deployed Server
After=network.target
StartLimitIntervalSec=0
[Service]
Environment=PATH=/home/$(whoami)/.local/bin/
Type=simple
Restart=always
User=$(whoami)
ExecStart=/home/$(whoami)/.local/bin/golemsp run
[Install]
WantedBy=multi-user.target
